# Client (optional): override the AI measurement endpoint. Defaults to /api/ai-measure
VITE_AI_MEASURE_URL=

# Server-side only (set on your hosting platform, NOT committed):
# ANTHROPIC_API_KEY=sk-ant-...
# AI_MEASURE_MODEL=claude-sonnet-4-6
